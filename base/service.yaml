apiVersion: v1
kind: Service
metadata:
  name: $(APP_NAME)
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: $(APP_NAME)
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
